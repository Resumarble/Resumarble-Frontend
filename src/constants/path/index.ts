export const AUTH_URL = Object.freeze({
  LOGIN: '/auth/login',
  JOIN: '/auth/join',
});

export const PATH = Object.freeze({
  ...AUTH_URL,
  ROOT: '/',
  MY_PAGE: '/mypage',
  RESUME: '/resume',
});
